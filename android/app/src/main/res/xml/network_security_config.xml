<?xml version="1.0" encoding="utf-8"?>
<!-- HTTPS everywhere, with exactly two development exceptions: the emulator's
     host loopback (10.0.2.2) and localhost, so a `go run ./cmd/server` on the
     workstation can be paired against. Production servers MUST be https. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
